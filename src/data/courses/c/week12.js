/**
 * StudyEarn AI — C Programming Course
 * Week 12: Final Capstone Project + Certificate 🎓
 * Hinglish + English bilingual, 4 sections, Noob to Pro
 */

export const C_WEEK_12 = {
  week: 12,
  title: 'Final Capstone — C Master Project + Certificate 🎓',
  title_en: 'Final Capstone — C Master Project + Certificate 🎓',
  description: '12 hafte ka safar complete! Ek production-ready C system banao jo sab kuch combine kare — aur C Programming Master certificate pao!',
  description_en: '12-week journey complete! Build a production-ready C system combining everything — and earn your C Programming Master certificate!',
  xpReward: 500,
  sections: [
    {
      id: 'c-w12-s1',
      title: 'Complete C Course Review — Noob se Pro tak',
      title_en: 'Complete C Course Review — From Noob to Pro',
      emoji: '📚',
      content: `## 12 Weeks ka Safar — Kya Seekha?

Congratulations! 12 hafte mein tumne C programming ki complete journey ki hai. Ab ek comprehensive review karte hain.

### Month 1 — C Foundation (Weeks 1-4)

\`\`\`
Week 1: C Basics
  ✅ Variables, data types (int, float, double, char)
  ✅ printf/scanf — I/O
  ✅ Operators — arithmetic, relational, logical, bitwise
  ✅ Compilation process

Week 2: Control Flow
  ✅ if/else/else if, ternary operator
  ✅ switch statement, fall-through
  ✅ for, while, do-while loops
  ✅ break, continue

Week 3: Functions, Arrays, Strings
  ✅ Function prototypes, return values, scope
  ✅ Pass by value vs pointer
  ✅ Recursion — base case, memoization, Tower of Hanoi
  ✅ Arrays — 1D/2D, sorting, searching
  ✅ Strings — string.h functions, fgets, Caesar cipher

Week 4: Pointers & Memory
  ✅ Pointer basics — &, *, pointer arithmetic
  ✅ Pass by reference, double pointers
  ✅ Dynamic memory — malloc, calloc, realloc, free
  ✅ Memory errors — leak, dangling, overflow
  ✅ Function pointers — callbacks, qsort
\`\`\`

### Month 2 — Data Structures (Weeks 5-8)

\`\`\`
Week 5: Structures & Linked Lists
  ✅ struct — typedef, nested, array of structs
  ✅ union — tagged union, type punning
  ✅ enum — named constants, bit flags
  ✅ Linked Lists — singly, doubly, merge sort, Floyd's cycle

Week 6: File I/O & Error Handling
  ✅ Text files — fopen, fprintf, fgets, fseek
  ✅ Binary files — fread, fwrite, random access
  ✅ Custom file format with header + magic bytes
  ✅ Error handling — errno, custom codes, goto cleanup
  ✅ File-based database system

Week 7: Preprocessor & Bitwise
  ✅ #define, #ifdef, conditional compilation
  ✅ Function-like macros, do-while(0) trick
  ✅ X-Macro pattern, variadic functions
  ✅ Inline functions
  ✅ Bitwise operations — AND/OR/XOR/shifts
  ✅ Bit manipulation tricks, bitfields

Week 8: Stack, Queue, Hash Table
  ✅ Stack — array + linked list, postfix eval, brackets
  ✅ Queue — circular, priority queue (min-heap)
  ✅ Hash table — chaining, open addressing
  ✅ BFS with queue, word frequency counter
\`\`\`

### Month 3 — Advanced Algorithms (Weeks 9-11)

\`\`\`
Week 9: Trees
  ✅ Binary tree traversals — inorder, preorder, postorder, BFS
  ✅ Tree properties — height, diameter, balance
  ✅ BST — insert, search, delete (3 cases)
  ✅ BST — floor/ceil, kth smallest, range queries
  ✅ AVL tree — rotations, guaranteed O(log n)

Week 10: Sorting Algorithms
  ✅ O(n²): Bubble, Selection, Insertion, Shell
  ✅ O(n logn): Merge Sort, Quick Sort (3-way, random pivot)
  ✅ O(n): Heap Sort, Counting Sort, Radix Sort
  ✅ When to use what — decision guide
  ✅ stdlib qsort, bsearch

Week 11: Graphs
  ✅ Adjacency matrix vs adjacency list
  ✅ DFS, BFS — traversal, cycle detection, components
  ✅ Dijkstra's shortest path (matrix + heap versions)
  ✅ Topological sort (DFS + Kahn's BFS)
  ✅ Prim's MST
  ✅ Strongly connected components
\`\`\`

### Big-O Complexity Cheat Sheet

\`\`\`
Data Structure     Access   Search   Insert   Delete
─────────────────────────────────────────────────────
Array              O(1)     O(n)     O(n)     O(n)
Linked List        O(n)     O(n)     O(1)     O(1)*
Stack (array)      O(1)     O(n)     O(1)     O(1)
Queue (linked)     O(1)     O(n)     O(1)     O(1)
Hash Table         O(1)     O(1)*    O(1)*    O(1)*
BST                O(logn)* O(logn)* O(logn)* O(logn)*
AVL Tree           O(logn)  O(logn)  O(logn)  O(logn)
Min-Heap           O(1)     O(n)     O(logn)  O(logn)
─────────────────────────────────────────────────────
* = average case

Sorting            Best     Average  Worst    Space
─────────────────────────────────────────────────────
Insertion Sort     O(n)     O(n²)    O(n²)    O(1)
Merge Sort         O(nlogn) O(nlogn) O(nlogn) O(n)
Quick Sort         O(nlogn) O(nlogn) O(n²)    O(logn)
Heap Sort          O(nlogn) O(nlogn) O(nlogn) O(1)
Counting Sort      O(n+k)   O(n+k)   O(n+k)   O(k)
\`\`\``,

      content_en: `## 12 Weeks ka Safar — Kya Seekha?

Congratulations! 12 hafte in tumne C programming ki complete journey ki hai. Ab ek comprehensive review karte hain.

### Month 1 — C Foundation (Weeks 1-4)

\`\`\`
Week 1: C Basics
  ✅ Variables, data types (int, float, double, char)
  ✅ printf/scanf — I/O
  ✅ Operators — arithmetic, relational, logical, bitwise
  ✅ Compilation process

Week 2: Control Flow
  ✅ if/else/else if, ternary operator
  ✅ switch statement, fall-through
  ✅ for, while, do-while loops
  ✅ break, continue

Week 3: Functions, Arrays, Strings
  ✅ Function prototypes, return values, scope
  ✅ Pass by value vs pointer
  ✅ Recursion — base case, memoization, Tower of Hanoi
  ✅ Arrays — 1D/2D, sorting, searching
  ✅ Strings — string.h functions, fgets, Caesar cipher

Week 4: Pointers & Memory
  ✅ Pointer basics — &, *, pointer arithmetic
  ✅ Pass by reference, double pointers
  ✅ Dynamic memory — malloc, calloc, realloc, free
  ✅ Memory errors — leak, dangling, overflow
  ✅ Function pointers — callbacks, qsort
\`\`\`

### Month 2 — Data Structures (Weeks 5-8)

\`\`\`
Week 5: Structures & Linked Lists
  ✅ struct — typedef, nested, array of structs
  ✅ union — tagged union, type punning
  ✅ enum — named constants, bit flags
  ✅ Linked Lists — singly, doubly, merge sort, Floyd's cycle

Week 6: File I/O & Error Handling
  ✅ Text files — fopen, fprintf, fgets, fseek
  ✅ Binary files — fread, fwrite, random access
  ✅ Custom file format with header + magic bytes
  ✅ Error handling — errno, custom codes, goto cleanup
  ✅ File-based database system

Week 7: Preprocessor & Bitwise
  ✅ #define, #ifdef, conditional compilation
  ✅ Function-like macros, do-while(0) trick
  ✅ X-Macro pattern, variadic functions
  ✅ Inline functions
  ✅ Bitwise operations — AND/OR/XOR/shifts
  ✅ Bit manipulation tricks, bitfields

Week 8: Stack, Queue, Hash Table
  ✅ Stack — array + linked list, postfix eval, brackets
  ✅ Queue — circular, priority queue (min-heap)
  ✅ Hash table — chaining, open addressing
  ✅ BFS with queue, word frequency counter
\`\`\`

### Month 3 — Advanced Algorithms (Weeks 9-11)

\`\`\`
Week 9: Trees
  ✅ Binary tree traversals — inorder, preorder, postorder, BFS
  ✅ Tree properties — height, diameter, balance
  ✅ BST — insert, search, delete (3 cases)
  ✅ BST — floor/ceil, kth smallest, range queries
  ✅ AVL tree — rotations, guaranteed O(log n)

Week 10: Sorting Algorithms
  ✅ O(n²): Bubble, Selection, Insertion, Shell
  ✅ O(n logn): Merge Sort, Quick Sort (3-way, random pivot)
  ✅ O(n): Heap Sort, Counting Sort, Radix Sort
  ✅ When to use what — decision guide
  ✅ stdlib qsort, bsearch

Week 11: Graphs
  ✅ Adjacency matrix vs adjacency list
  ✅ DFS, BFS — traversal, cycle detection, components
  ✅ Dijkstra's shortest path (matrix + heap versions)
  ✅ Topological sort (DFS + Kahn's BFS)
  ✅ Prim's MST
  ✅ Strongly connected components
\`\`\`

### Big-O Complexity Cheat Sheet

\`\`\`
Data Structure     Access   Search   Insert   Delete
─────────────────────────────────────────────────────
Array              O(1)     O(n)     O(n)     O(n)
Linked List        O(n)     O(n)     O(1)     O(1)*
Stack (array)      O(1)     O(n)     O(1)     O(1)
Queue (linked)     O(1)     O(n)     O(1)     O(1)
Hash Table         O(1)     O(1)*    O(1)*    O(1)*
BST                O(logn)* O(logn)* O(logn)* O(logn)*
AVL Tree           O(logn)  O(logn)  O(logn)  O(logn)
Min-Heap           O(1)     O(n)     O(logn)  O(logn)
─────────────────────────────────────────────────────
* = average case

Sorting            Best     Average  Worst    Space
─────────────────────────────────────────────────────
Insertion Sort     O(n)     O(n²)    O(n²)    O(1)
Merge Sort         O(nlogn) O(nlogn) O(nlogn) O(n)
Quick Sort         O(nlogn) O(nlogn) O(n²)    O(logn)
Heap Sort          O(nlogn) O(nlogn) O(nlogn) O(1)
Counting Sort      O(n+k)   O(n+k)   O(n+k)   O(k)
\`\`\``,

      codeExample: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

// ── Quick review: test all major concepts ──
#define ARRAY_SZ(a) (sizeof(a)/sizeof(a[0]))

// 1. Recursive fibonacci (with memoization)
int memo[100];
int fib(int n){
    if(n<=1)return n;
    if(memo[n])return memo[n];
    return memo[n]=fib(n-1)+fib(n-2);
}

// 2. Generic stack
typedef struct{int*d;int t,cap;}Stack;
Stack*newStack(){Stack*s=malloc(sizeof*s);s->d=malloc(16*4);s->t=-1;s->cap=16;return s;}
void push(Stack*s,int v){if(s->t==s->cap-1){s->cap*=2;s->d=realloc(s->d,s->cap*4);}s->d[++s->t]=v;}
int  pop(Stack*s){return s->t<0?-1:s->d[s->t--];}
void freeStack(Stack*s){free(s->d);free(s);}

// 3. Hash map (word frequency)
#define HM_SZ 64
typedef struct E{char k[32];int v;struct E*n;}Entry;
typedef struct{Entry*b[HM_SZ];int cnt;}HashMap;
HashMap*newMap(){return calloc(1,sizeof(HashMap));}
static unsigned hsh(const char*s){unsigned h=5381;while(*s)h=h*33+(unsigned char)*s++;return h%HM_SZ;}
void mapSet(HashMap*m,const char*k,int v){unsigned i=hsh(k);for(Entry*e=m->b[i];e;e=e->n)if(!strcmp(e->k,k)){e->v=v;return;}Entry*e=malloc(sizeof*e);strncpy(e->k,k,31);e->v=v;e->n=m->b[i];m->b[i]=e;m->cnt++;}
int* mapGet(HashMap*m,const char*k){for(Entry*e=m->b[hsh(k)];e;e=e->n)if(!strcmp(e->k,k))return&e->v;return NULL;}
void freeMap(HashMap*m){for(int i=0;i<HM_SZ;i++){Entry*e=m->b[i],*t;while(e){t=e->n;free(e);e=t;}}free(m);}

// 4. Quick sort
void qsSwap(int*a,int*b){int t=*a;*a=*b;*b=t;}
int qsPart(int a[],int lo,int hi){int ri=lo+rand()%(hi-lo+1);qsSwap(&a[ri],&a[hi]);int p=a[hi],i=lo-1;for(int j=lo;j<hi;j++)if(a[j]<=p)qsSwap(&a[++i],&a[j]);qsSwap(&a[i+1],&a[hi]);return i+1;}
void quickSort(int a[],int lo,int hi){if(lo<hi){int p=qsPart(a,lo,hi);quickSort(a,lo,p-1);quickSort(a,p+1,hi);}}

// 5. BST
typedef struct BST{int d;struct BST*l,*r;}BST;
BST*bstIns(BST*r,int v){if(!r){BST*n=malloc(sizeof*n);n->d=v;n->l=n->r=NULL;return n;}if(v<r->d)r->l=bstIns(r->l,v);else if(v>r->d)r->r=bstIns(r->r,v);return r;}
BST*bstSrch(BST*r,int v){while(r){if(v==r->d)return r;r=v<r->d?r->l:r->r;}return NULL;}
void bstIO(BST*r){if(!r)return;bstIO(r->l);printf("%d ",r->d);bstIO(r->r);}
void bstFree(BST*r){if(!r)return;bstFree(r->l);bstFree(r->r);free(r);}

int main(){
    printf("=== 12-Week C Review ===\\n\\n");

    // Fibonacci
    memset(memo,0,sizeof(memo));
    printf("Fibonacci: ");
    for(int i=0;i<=10;i++) printf("fib(%d)=%d ",i,fib(i));
    printf("\\n");

    // Stack
    Stack*stk=newStack();
    printf("\\nStack: ");
    for(int i=1;i<=5;i++) push(stk,i*10);
    while(stk->t>=0) printf("%d ",pop(stk));
    printf("(LIFO)\\n");
    freeStack(stk);

    // Hash map
    HashMap*freq=newMap();
    char *words[]={"C","is","great","C","is","fast","C","rocks"};
    for(int i=0;i<8;i++){
        int*v=mapGet(freq,words[i]);
        if(v)(*v)++; else mapSet(freq,words[i],1);
    }
    printf("\\nWord Frequency:\\n");
    for(int i=0;i<HM_SZ;i++)
        for(Entry*e=freq->b[i];e;e=e->n)
            printf("  %-10s: %d\\n",e->k,e->v);
    freeMap(freq);

    // Quick sort
    int arr[]={64,34,25,12,22,11,90,45,78};
    int n=ARRAY_SZ(arr);
    quickSort(arr,0,n-1);
    printf("\\nSorted: ");
    for(int i=0;i<n;i++) printf("%d ",arr[i]);
    printf("\\n");

    // BST
    BST*tree=NULL;
    int vals[]={50,30,70,20,40,60,80};
    for(int i=0;i<7;i++) tree=bstIns(tree,vals[i]);
    printf("\\nBST Inorder: "); bstIO(tree); printf("\\n");
    printf("Search 40: %s\\n",bstSrch(tree,40)?"Found":"Not found");
    bstFree(tree);

    printf("\\n✅ All concepts working! Ready for final project.\\n");
    return 0;
}`,

      codeExample_en: `/* See above — complete review of all 12 weeks */`,

      task: {
        description: 'Review challenge — sab topics test karo: (1) "Complexity Quiz" — 10 different code snippets ki time/space complexity identify karo. (2) "Data Structure Picker" — 5 different problems ke liye best data structure explain karo with justification. (3) "Debug the Code" — 5 buggy C programs fix karo (memory leak, null pointer, buffer overflow, off-by-one, dangling pointer). (4) "Implement from scratch" — HashMap aur Merge Sort sirf memory mein (no paper, no reference) — test sab cases.',
        description_en: 'Review challenge — test all topics: (1) "Complexity Quiz" — identify the time/space complexity of 10 different code snippets. (2) "Data Structure Picker" — explain the best data structure for 5 different problems with justification. (3) "Debug the Code" — fix 5 buggy C programs (memory leak, null pointer, buffer overflow, off-by-one, dangling pointer). (4) "Implement from scratch" — implement HashMap and Merge Sort from memory — test all edge cases.',
        hint: 'Complexity quiz: look for nested loops (O(n²)), log splits (O(logn)), hash access (O(1) avg). Debug: use printf to trace values, check every malloc for NULL, check every array index < size.',
        hint_en: 'Complexity quiz: look for nested loops (O(n²)), logarithmic splits (O(logn)), hash access (O(1) avg). Debugging: use printf to trace values, check every malloc for NULL, check every array index is < size.',
      },
      quiz: [
        {
          q: 'C programming seekhne ka sabse practical benefit kya hai?',
          options: [
            'Sirf academic',
            'Memory management samajh aata hai — Python/Java/Go sab ke garbage collectors ko mentally model kar sakte ho. Systems programming, embedded development, OS internals, compilers — sab C se hi samajhe jaate hain.',
            'C fast hoga hamesha',
            'Jobs ke liye nahi chahiye',
          ],
          correct: 1,
          explanation: 'C = computer ka native language. Seekhne ke baad: (1) Koi bhi language ka internal samajh aata hai — Python C mein likha hai! (2) Debugging skills improve — memory model clear hoti hai. (3) Performance optimization — know when/why to optimize. (4) Systems programming door nahi lagta. (5) Competitive programming faster. (6) FAANG interviews mein strong foundation.',
          q_en: 'What is the most practical benefit of learning C programming?',
          options_en: [
            'Only academic',
            'You understand memory management — you can mentally model the garbage collectors of Python/Java/Go. Systems programming, embedded development, OS internals, compilers — all understood through C.',
            'C will always be the fastest',
            'Not needed for jobs',
          ],
          explanation_en: 'C = the native language of computers. After learning: (1) You understand any language\'s internals — Python is written in C! (2) Debugging skills improve — your mental memory model is clear. (3) Performance optimisation — you know when and why to optimise. (4) Systems programming no longer feels distant. (5) Competitive programming becomes faster. (6) Strong foundation for FAANG interviews.',
        },
        {
          q: 'C mein sabse important concept jo industry mein hamesha use hota hai?',
          options: [
            'Bubble sort',
            'Pointers aur memory management — Linux kernel, embedded systems, databases, networking sab mein critical. Manual memory = performance control. Bina pointers ke C ka koi matlab nahi.',
            'printf aur scanf',
            'switch statement',
          ],
          correct: 1,
          explanation: 'C ka superpower = direct memory control. Pointers: arrays, strings, dynamic DS, function callbacks, system calls sab enable karte hain. malloc/free: OS se memory maango, kaam ho toh wapas do. Industry: Linux device drivers (pointer-heavy), embedded (every byte matters), database buffer management (precise memory). C ke bina modern computing infrastructure nahi hoti.',
          q_en: 'What is the most important concept in C that is used throughout industry?',
          options_en: [
            'Bubble sort',
            'Pointers and memory management — critical in the Linux kernel, embedded systems, databases, networking. Manual memory = performance control. Without pointers, C has no meaning.',
            'printf and scanf',
            'switch statement',
          ],
          explanation_en: 'C\'s superpower = direct memory control. Pointers enable: arrays, strings, dynamic data structures, function callbacks, system calls. malloc/free: request memory from the OS, return it when done. Industry: Linux device drivers (pointer-heavy), embedded (every byte matters), database buffer management (precise memory). Modern computing infrastructure would not exist without C.',
        },
        {
          q: '12 weeks mein C seekhne ke baad next step kya hona chahiye?',
          options: [
            'C hi use karte raho hamesha',
            'C++ seekho (OOP + templates + STL), ya Systems Programming (OS concepts, sockets, threads), ya Competitive Programming (algorithms apply karo), ya Embedded C (Arduino/STM32).',
            'Doosri language mat seekho',
            'Sirf practice karo',
          ],
          correct: 1,
          explanation: 'C foundation ke baad pathways: (1) C++ — OOP, templates, STL, modern C++17/20. (2) Systems Programming — fork/exec, sockets, POSIX threads. (3) Competitive Programming — apply algorithms on Codeforces/LeetCode. (4) Embedded — microcontrollers, RTOS, hardware interfacing. (5) OS Development — write your own bootloader! All roads lead from C. Congratulations on completing this journey!',
          q_en: 'After learning C in 12 weeks, what should the next step be?',
          options_en: [
            'Always keep using only C',
            'Learn C++ (OOP + templates + STL), or Systems Programming (OS concepts, sockets, threads), or Competitive Programming (apply algorithms), or Embedded C (Arduino/STM32).',
            'Do not learn another language',
            'Just keep practising',
          ],
          explanation_en: 'Pathways after a C foundation: (1) C++ — OOP, templates, STL, modern C++17/20. (2) Systems Programming — fork/exec, sockets, POSIX threads. (3) Competitive Programming — apply algorithms on Codeforces/LeetCode. (4) Embedded — microcontrollers, RTOS, hardware interfacing. (5) OS Development — write your own bootloader! All roads lead from C. Congratulations on completing this journey!',
        },
      ],
    },

    // ── SECTION 2 ──────────────────────────────────────────────────────
    {
      id: 'c-w12-s2',
      title: 'Final Capstone Project — StudyEarn Student Portal',
      title_en: 'Final Capstone Project — StudyEarn Student Portal',
      emoji: '🏗️',
      content: `## Final Project — Sab Kuch Ek Mein!

12 hafte mein jo seekha — sab ek production-ready system mein use karo!

### Project Specification

\`\`\`
StudyEarn Student Portal — Complete CLI System

Features:
1. Student Registration & Login
2. Course Enrollment
3. Progress Tracking
4. Grade Management
5. Leaderboard
6. Search & Filter
7. Data Persistence (binary + CSV)
8. Analytics Dashboard

Architecture:
  ┌─────────────────────────────────────────────┐
  │           StudyEarn Student Portal           │
  ├─────────────────────────────────────────────┤
  │  Core Data Structures                        │
  │  ┌────────────┐ ┌──────────┐ ┌───────────┐  │
  │  │ Hash Table │ │  BST     │ │  Graph    │  │
  │  │(fast lookup│ │(sorted   │ │(course    │  │
  │  │ by roll)   │ │ listing) │ │ prereqs)  │  │
  │  └────────────┘ └──────────┘ └───────────┘  │
  ├─────────────────────────────────────────────┤
  │  Algorithms                                  │
  │  • Quick Sort — leaderboard by score         │
  │  • Dijkstra  — shortest course path          │
  │  • Topo Sort — prerequisite order            │
  │  • BFS/DFS   — recommendation engine         │
  ├─────────────────────────────────────────────┤
  │  Persistence                                 │
  │  • Binary files — student records            │
  │  • CSV export  — reports                     │
  │  • Log file    — audit trail                 │
  └─────────────────────────────────────────────┘
\`\`\`

### Core Data Structures Used

\`\`\`c
// Student record
typedef struct {
    uint32_t rollNo;
    char     name[60];
    char     email[60];
    float    marks[10];   // up to 10 courses
    float    gpa;
    char     grade;
    uint8_t  enrolled[10]; // which courses enrolled
    uint8_t  completed[10];
    int      totalXP;
    uint8_t  active;
} Student;

// BST for sorted student listing
// Hash table for O(1) lookup by roll number
// Graph for course prerequisite system
// Priority queue for leaderboard updates
// File I/O for persistence

// Menu system
typedef enum {
    MENU_REGISTER = 1,
    MENU_LOGIN,
    MENU_VIEW_PROFILE,
    MENU_ENROLL,
    MENU_UPDATE_MARKS,
    MENU_LEADERBOARD,
    MENU_SEARCH,
    MENU_ANALYTICS,
    MENU_EXPORT,
    MENU_LOGOUT,
    MENU_EXIT = 0,
} MenuChoice;
\`\`\`

### Project Structure

\`\`\`
studyearn/
├── main.c          ← entry point, menu loop
├── student.h       ← Student struct, prototypes
├── student.c       ← CRUD operations
├── database.h      ← DB layer (binary files)
├── database.c      ← file operations
├── bst.h           ← BST for sorted listing
├── hashtable.h     ← O(1) lookup
├── sort.h          ← sorting functions
├── analytics.h     ← statistics, reporting
├── Makefile        ← build system
└── data/
    ├── students.bin  ← binary student data
    ├── students.idx  ← index file
    └── audit.log     ← operation log
\`\`\``,

      content_en: `## Final Project — Sab Kuch Ek In!

12 hafte in jo seekha — sab ek production-ready system in use do!

### Project Specification

\`\`\`
StudyEarn Student Portal — Complete CLI System

Features:
1. Student Registration & Login
2. Course Enrollment
3. Progress Tracking
4. Grade Management
5. Leaderboard
6. Search & Filter
7. Data Persistence (binary + CSV)
8. Analytics Dashboard

Architecture:
  ┌─────────────────────────────────────────────┐
  │           StudyEarn Student Portal           │
  ├─────────────────────────────────────────────┤
  │  Core Data Structures                        │
  │  ┌────────────┐ ┌──────────┐ ┌───────────┐  │
  │  │ Hash Table │ │  BST     │ │  Graph    │  │
  │  │(fast lookup│ │(sorted   │ │(course    │  │
  │  │ by roll)   │ │ listing) │ │ prereqs)  │  │
  │  └────────────┘ └──────────┘ └───────────┘  │
  ├─────────────────────────────────────────────┤
  │  Algorithms                                  │
  │  • Quick Sort — leaderboard by score         │
  │  • Dijkstra  — shortest course path          │
  │  • Topo Sort — prerequisite order            │
  │  • BFS/DFS   — recommendation engine         │
  ├─────────────────────────────────────────────┤
  │  Persistence                                 │
  │  • Binary files — student records            │
  │  • CSV export  — reports                     │
  │  • Log file    — audit trail                 │
  └─────────────────────────────────────────────┘
\`\`\`

### Core Data Structures Used

\`\`\`c
// Student record
typedef struct {
    uint32_t rollNo;
    char     name[60];
    char     email[60];
    float    marks[10];   // up to 10 courses
    float    gpa;
    char     grade;
    uint8_t  enrolled[10]; // which courses enrolled
    uint8_t  completed[10];
    int      totalXP;
    uint8_t  active;
} Student;

// BST for sorted student listing
// Hash table for O(1) lookup by roll number
// Graph for course prerequisite system
// Priority queue for leaderboard updates
// File I/O for persistence

// Menu system
typedef enum {
    MENU_REGISTER = 1,
    MENU_LOGIN,
    MENU_VIEW_PROFILE,
    MENU_ENROLL,
    MENU_UPDATE_MARKS,
    MENU_LEADERBOARD,
    MENU_SEARCH,
    MENU_ANALYTICS,
    MENU_EXPORT,
    MENU_LOGOUT,
    MENU_EXIT = 0,
} MenuChoice;
\`\`\`

### Project Structure

\`\`\`
studyearn/
├── main.c          ← entry point, menu loop
├── student.h       ← Student struct, prototypes
├── student.c       ← CRUD operations
├── database.h      ← DB layer (binary files)
├── database.c      ← file operations
├── bst.h           ← BST for sorted listing
├── hashtable.h     ← O(1) lookup
├── sort.h          ← sorting functions
├── analytics.h     ← statistics, reporting
├── Makefile        ← build system
└── data/
    ├── students.bin  ← binary student data
    ├── students.idx  ← index file
    └── audit.log     ← operation log
\`\`\``,

      codeExample: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <stdint.h>

// ════════════════════════════════════════════════════════
// STUDYEARN STUDENT PORTAL — COMPLETE IMPLEMENTATION
// Uses: struct, BST, hash table, sort, file I/O, graphs
// ════════════════════════════════════════════════════════

#define MAX_STUDENTS 500
#define NUM_COURSES  5
#define DB_FILE      "students.bin"
#define LOG_FILE     "audit.log"

char *COURSE_NAMES[] = {"C Basics","Data Structures","Algorithms","Systems","Projects"};
float COURSE_CREDITS[] = {4.0f, 3.0f, 3.0f, 3.0f, 2.0f};

typedef struct {
    uint32_t roll;
    char     name[50];
    char     email[40];
    float    marks[NUM_COURSES];
    float    gpa;
    int      xp;
    uint8_t  enrolled;  // bit flags per course
    uint8_t  active;
} Student;

// ── In-memory database ──
Student db[MAX_STUDENTS];
int     dbCount = 0;
int     nextRoll = 1001;

// ── Logging ──
void logOp(const char *op, const char *detail) {
    FILE *f = fopen(LOG_FILE, "a");
    if (!f) return;
    time_t t = time(NULL); struct tm *tm = localtime(&t);
    fprintf(f, "[%04d-%02d-%02d %02d:%02d:%02d] %-10s %s\n",
            tm->tm_year+1900, tm->tm_mon+1, tm->tm_mday,
            tm->tm_hour, tm->tm_min, tm->tm_sec, op, detail);
    fclose(f);
}

// ── GPA calculation ──
void calcGPA(Student *s) {
    float totalPoints = 0, totalCredits = 0;
    for (int i = 0; i < NUM_COURSES; i++) {
        if (s->enrolled & (1<<i)) {
            totalPoints  += s->marks[i] * COURSE_CREDITS[i];
            totalCredits += COURSE_CREDITS[i];
        }
    }
    s->gpa = totalCredits > 0 ? totalPoints / totalCredits : 0;
    s->gpa = s->gpa > 10 ? 10 : s->gpa;
    // XP based on GPA
    s->xp = (int)(s->gpa * 100);
}

// ── CRUD Operations ──
int registerStudent(const char *name, const char *email) {
    if (dbCount >= MAX_STUDENTS) { puts("DB full!"); return -1; }
    Student *s = &db[dbCount++];
    memset(s, 0, sizeof(Student));
    s->roll   = nextRoll++;
    s->active = 1;
    strncpy(s->name,  name,  49);
    strncpy(s->email, email, 39);
    char buf[100]; snprintf(buf, 100, "Roll:%u %s", s->roll, s->name);
    logOp("REGISTER", buf);
    return s->roll;
}

Student* findByRoll(int roll) {
    for (int i = 0; i < dbCount; i++)
        if (db[i].roll == (uint32_t)roll && db[i].active)
            return &db[i];
    return NULL;
}

Student* findByName(const char *name) {
    for (int i = 0; i < dbCount; i++)
        if (db[i].active && strstr(db[i].name, name))
            return &db[i];
    return NULL;
}

void enrollCourse(Student *s, int courseIdx) {
    if (courseIdx < 0 || courseIdx >= NUM_COURSES) { puts("Invalid course!"); return; }
    if (s->enrolled & (1<<courseIdx)) { puts("Already enrolled!"); return; }
    s->enrolled |= (1<<courseIdx);
    char buf[60]; snprintf(buf,60,"Roll:%u in %s",s->roll,COURSE_NAMES[courseIdx]);
    logOp("ENROLL", buf);
    printf("✅ Enrolled in %s\\n", COURSE_NAMES[courseIdx]);
}

void updateMarks(Student *s, int courseIdx, float marks) {
    if (!(s->enrolled & (1<<courseIdx))) { puts("Not enrolled!"); return; }
    s->marks[courseIdx] = marks > 10 ? 10 : (marks < 0 ? 0 : marks);
    calcGPA(s);
    char buf[80]; snprintf(buf,80,"Roll:%u %s=%.1f GPA=%.2f",s->roll,COURSE_NAMES[courseIdx],marks,s->gpa);
    logOp("MARKS", buf);
    printf("✅ Marks updated. GPA: %.2f\\n", s->gpa);
}

// ── Display ──
void printStudent(const Student *s) {
    printf("┌────────────────────────────────────────┐\\n");
    printf("│ Roll: %-6u  %-27s │\\n", s->roll, s->name);
    printf("│ Email: %-33s │\\n", s->email);
    printf("├────────────────────────────────────────┤\\n");
    printf("│ Course Marks:                          │\\n");
    for (int i = 0; i < NUM_COURSES; i++) {
        if (s->enrolled & (1<<i))
            printf("│   %-20s : %4.1f/10          │\\n", COURSE_NAMES[i], s->marks[i]);
    }
    printf("├────────────────────────────────────────┤\\n");
    printf("│ GPA: %-6.2f  XP: %-5d  Grade: %c       │\\n",
           s->gpa, s->xp, s->gpa>=9?'O':s->gpa>=7.5?'A':s->gpa>=6?'B':s->gpa>=4.5?'C':'F');
    printf("└────────────────────────────────────────┘\\n");
}

// ── Leaderboard (Quick Sort) ──
int cmpGPA(const void *a, const void *b) {
    const Student *sa = (const Student*)a, *sb = (const Student*)b;
    if (sb->gpa > sa->gpa) return 1;
    if (sb->gpa < sa->gpa) return -1;
    return 0;
}

void showLeaderboard(int top) {
    Student sorted[MAX_STUDENTS];
    int cnt = 0;
    for (int i = 0; i < dbCount; i++)
        if (db[i].active) sorted[cnt++] = db[i];
    qsort(sorted, cnt, sizeof(Student), cmpGPA);
    printf("\\n╔══════════════════════════════════════════╗\\n");
    printf("║          STUDYEARN LEADERBOARD           ║\\n");
    printf("╠══════════════════════════════════════════╣\\n");
    printf("║ %-4s %-20s %-6s %-6s ║\\n","Rank","Name","GPA","XP");
    printf("╠══════════════════════════════════════════╣\\n");
    for (int i = 0; i < (cnt<top?cnt:top); i++) {
        char medal[] = "   ";
        if (i==0) strcpy(medal,"🥇 ");
        else if (i==1) strcpy(medal,"🥈 ");
        else if (i==2) strcpy(medal,"🥉 ");
        printf("║ %s#%-2d %-17s %5.2f  %-5d ║\\n",
               medal, i+1, sorted[i].name, sorted[i].gpa, sorted[i].xp);
    }
    printf("╚══════════════════════════════════════════╝\\n");
}

// ── Analytics ──
void analytics() {
    if (!dbCount) { puts("No students!"); return; }
    float sumGPA=0, maxGPA=0, minGPA=10;
    int pass=0, active=0;
    float courseAvg[NUM_COURSES]={0}; int courseCount[NUM_COURSES]={0};
    for (int i=0;i<dbCount;i++) {
        if(!db[i].active) continue; active++;
        sumGPA += db[i].gpa;
        if(db[i].gpa>maxGPA) maxGPA=db[i].gpa;
        if(db[i].gpa<minGPA) minGPA=db[i].gpa;
        if(db[i].gpa>=4.5) pass++;
        for(int j=0;j<NUM_COURSES;j++)
            if(db[i].enrolled&(1<<j)){courseAvg[j]+=db[i].marks[j];courseCount[j]++;}
    }
    printf("\\n=== ANALYTICS DASHBOARD ===\\n");
    printf("Active students   : %d\\n",active);
    printf("Average GPA       : %.2f\\n",active?sumGPA/active:0);
    printf("Highest GPA       : %.2f\\n",maxGPA);
    printf("Lowest GPA        : %.2f\\n",minGPA);
    printf("Pass rate         : %.1f%%\\n",active?(float)pass/active*100:0);
    printf("\\nCourse-wise averages:\\n");
    for(int i=0;i<NUM_COURSES;i++)
        printf("  %-20s: %.2f/10 (%d students)\\n",
               COURSE_NAMES[i],courseCount[i]?courseAvg[i]/courseCount[i]:0,courseCount[i]);
}

// ── Persistence ──
void saveDB() {
    FILE *f=fopen(DB_FILE,"wb");
    if(!f){perror("save");return;}
    fwrite(&dbCount,sizeof(int),1,f);
    fwrite(&nextRoll,sizeof(int),1,f);
    fwrite(db,sizeof(Student),dbCount,f);
    fclose(f);
    printf("✅ Saved %d records\\n",dbCount);
    logOp("SAVE","database");
}

void loadDB() {
    FILE *f=fopen(DB_FILE,"rb");
    if(!f){printf("No existing data.\\n");return;}
    fread(&dbCount,sizeof(int),1,f);
    fread(&nextRoll,sizeof(int),1,f);
    fread(db,sizeof(Student),dbCount,f);
    fclose(f);
    printf("✅ Loaded %d records\\n",dbCount);
}

void exportCSV() {
    FILE *f=fopen("report.csv","w");
    fprintf(f,"Roll,Name,GPA,XP,Grade\\n");
    for(int i=0;i<dbCount;i++)
        if(db[i].active)
            fprintf(f,"%u,%s,%.2f,%d,%c\\n",db[i].roll,db[i].name,db[i].gpa,db[i].xp,
                    db[i].gpa>=9?'O':db[i].gpa>=7.5?'A':db[i].gpa>=6?'B':'C');
    fclose(f); puts("✅ Exported to report.csv");
}

int main() {
    printf("╔══════════════════════════════════════════╗\\n");
    printf("║    🎓 STUDYEARN STUDENT PORTAL v1.0      ║\\n");
    printf("╚══════════════════════════════════════════╝\\n\\n");

    loadDB();

    // Demo: add sample students
    if (dbCount == 0) {
        printf("Setting up demo data...\\n");
        int r1=registerStudent("Rahul Sharma","rahul@email.com");
        int r2=registerStudent("Priya Patel","priya@email.com");
        int r3=registerStudent("Aryan Singh","aryan@email.com");

        Student *s1=findByRoll(r1);
        enrollCourse(s1,0); enrollCourse(s1,1); enrollCourse(s1,2);
        updateMarks(s1,0,8.5f); updateMarks(s1,1,9.0f); updateMarks(s1,2,7.5f);

        Student *s2=findByRoll(r2);
        enrollCourse(s2,0); enrollCourse(s2,1); enrollCourse(s2,2); enrollCourse(s2,3);
        updateMarks(s2,0,9.5f); updateMarks(s2,1,9.2f); updateMarks(s2,2,8.8f); updateMarks(s2,3,9.0f);

        Student *s3=findByRoll(r3);
        enrollCourse(s3,0); enrollCourse(s3,1);
        updateMarks(s3,0,7.0f); updateMarks(s3,1,6.5f);
    }

    // Demo operations
    printf("\\n=== Student Profiles ===\\n");
    for(int i=0;i<dbCount;i++) if(db[i].active) printStudent(&db[i]);

    showLeaderboard(10);
    analytics();

    saveDB();
    exportCSV();

    printf("\\n=== Audit Log ===\\n");
    FILE *log=fopen(LOG_FILE,"r");
    if(log){char line[200];while(fgets(line,200,log))printf("  %s",line);fclose(log);}

    printf("\\n✅ Session complete! Data saved.\\n");
    return 0;
}`,

      codeExample_en: `/* See above — complete Student Portal system */`,

      task: {
        description: 'Final Capstone — Complete Student Portal upgrade karo: (1) Interactive menu implement karo — full do-while menu loop with all operations. (2) "Course Graph" — prerequisites model karo (C Basics → DS → Algorithms), topological sort se valid enrollment order suggest karo, invalid enrollment prevent karo. (3) "Smart Search" — partial name, roll number, GPA range se search karo. (4) "Grade Prediction" — current marks se predicted final GPA calculate karo, improvement suggestions do. (5) "Full Test Suite" — edge cases test karo: empty DB, single student, all same GPA.',
        description_en: 'Final Capstone — Upgrade the complete Student Portal: (1) Implement an interactive menu — full do-while menu loop with all operations. (2) "Course Graph" — model prerequisites (C Basics → DS → Algorithms), use topological sort to suggest a valid enrollment order, prevent invalid enrollments. (3) "Smart Search" — search by partial name, roll number, or GPA range. (4) "Grade Prediction" — calculate predicted final GPA from current marks and give improvement suggestions. (5) "Full Test Suite" — test edge cases: empty DB, single student, all same GPA.',
        hint: 'Menu: do{print_menu();scanf("%d",&choice);switch(choice){...}}while(choice!=0). Course graph: directed graph, DFS cycle check on enrollment. Smart search: loop through all students, multiple match criteria. Grade prediction: weighted average with partial completion.',
        hint_en: 'Menu: do{print_menu();scanf("%d",&choice);switch(choice){...}}while(choice!=0). Course graph: directed graph, DFS cycle check on enrollment. Smart search: loop through all students, check multiple match criteria. Grade prediction: weighted average assuming current marks continue.',
      },
      quiz: [
        {
          q: 'Production-ready C code mein sabse important practice kya hai?',
          options: [
            'Sirf comments likhna',
            'Defensive programming — har function ka input validate karo, har malloc check karo for NULL, har file operation check karo, proper error codes return karo, resources hamesha cleanup karo.',
            'Sirf fast code likhna',
            'Sirf test karna',
          ],
          correct: 1,
          explanation: 'Production C: (1) Input validation — NULL checks, range checks. (2) Resource management — every malloc has a free, every fopen has fclose. (3) Error propagation — return error codes, caller handles. (4) Defensive macros — ASSERT for invariants. (5) Memory tools — Valgrind in CI/CD. (6) const-correctness. (7) No global state (or minimal). Linux kernel follows all these rigorously.',
          q_en: 'What is the most important practice for production-ready C code?',
          options_en: [
            'Just writing comments',
            'Defensive programming — validate every function input, check every malloc for NULL, check every file operation, return proper error codes, always clean up resources.',
            'Just writing fast code',
            'Just testing',
          ],
          explanation_en: 'Production C: (1) Input validation — NULL checks, range checks. (2) Resource management — every malloc has a free, every fopen has fclose. (3) Error propagation — return error codes, let the caller handle them. (4) Defensive macros — ASSERT for invariants. (5) Memory tools — Valgrind in CI/CD. (6) const-correctness. (7) No (or minimal) global state. The Linux kernel follows all of these rigorously.',
        },
        {
          q: 'C program mein memory layout kya hota hai?',
          options: [
            'Sirf ek memory area',
            'Text (code), Data (globals/static), Heap (malloc — grows up), Stack (local vars, function calls — grows down). Pointer arithmetic isi model se kaam karta hai.',
            'Stack sirf hoti hai',
            'OS manage karta hai sab',
          ],
          correct: 1,
          explanation: 'C memory segments: (1) Text: program code (read-only). (2) Data: initialized globals/static. (3) BSS: uninitialized globals (zero by OS). (4) Heap: dynamic allocation (malloc grows upward). (5) Stack: local variables, return addresses (grows downward). Stack overflow: stack grows into heap territory. Buffer overflow: stack frame corrupted. Understanding this = understanding segfaults!',
          q_en: 'What is the memory layout of a C program?',
          options_en: [
            'Just one memory area',
            'Text (code), Data (globals/static), Heap (malloc — grows up), Stack (local vars, function calls — grows down). Pointer arithmetic works within this model.',
            'Only the stack exists',
            'The OS manages everything',
          ],
          explanation_en: 'C memory segments: (1) Text: program code (read-only). (2) Data: initialised globals/static. (3) BSS: uninitialised globals (zeroed by OS). (4) Heap: dynamic allocation (malloc grows upward). (5) Stack: local variables, return addresses (grows downward). Stack overflow: stack grows into heap territory. Buffer overflow: stack frame corrupted. Understanding this = understanding segfaults!',
        },
        {
          q: 'C seekhna C++, Java, Python seekhne se pehle kyun valuable hai?',
          options: [
            'C zyada popular hai',
            'C = baaki languages ka foundation. Memory model, function calls, pointers — C mein explicit jo doosron mein hidden hai. Ek baar C samajh lo, baki languages ki internals clear ho jaati hain.',
            'C zyada jobs deta hai',
            'Sirf gamers ke liye useful',
          ],
          correct: 1,
          explanation: 'C foundation advantage: (1) Python\'s list = dynamic array (like our malloc-based array). (2) Java\'s JVM = C/C++ se likha gaya. (3) C++ is a superset of C. (4) Go\'s goroutines = C threads pe based. (5) Rust\'s ownership = C ke memory bugs solve karta hai. (6) JavaScript engines (V8) = C++ mein. C seekhne ke baad: har language ki "magic" samajh aati hai — you\'re not afraid of internals anymore!',
          q_en: 'Why is learning C valuable before learning C++, Java, or Python?',
          options_en: [
            'C is more popular',
            'C = the foundation of other languages. Memory model, function calls, pointers — explicit in C but hidden in others. Once you understand C, the internals of other languages become clear.',
            'C gives more jobs',
            'Only useful for gamers',
          ],
          explanation_en: "C foundation advantage: (1) Python's list = dynamic array (like our malloc-based array). (2) Java's JVM = written in C/C++. (3) C++ is a superset of C. (4) Go's goroutines are based on C threads. (5) Rust's ownership model solves C's memory bugs. (6) JavaScript engines (V8) = C++. After learning C: the 'magic' of every language becomes clear — you're no longer afraid of internals!",
        },
      ],
    },

    // ── SECTION 3 ──────────────────────────────────────────────────────
    {
      id: 'c-w12-s3',
      title: 'Advanced C Topics — Industry-Level Concepts',
      title_en: 'Advanced C Topics — Industry-Level Concepts',
      emoji: '🔬',
      content: `## Advanced C — Industry Mein Kaam Aane Wali Cheezein!

### POSIX Threads — Multithreading in C

\`\`\`c
#include <pthread.h>

// Thread function
void* threadFunc(void *arg) {
    int id = *(int*)arg;
    printf("Thread %d running\\n", id);
    // do work...
    return NULL;
}

// Create and join threads
pthread_t threads[4];
int ids[4] = {1, 2, 3, 4};

for (int i = 0; i < 4; i++)
    pthread_create(&threads[i], NULL, threadFunc, &ids[i]);

for (int i = 0; i < 4; i++)
    pthread_join(threads[i], NULL);  // wait for all

// Mutex — protect shared data
pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;
int sharedCounter = 0;

void* safeIncrement(void *arg) {
    pthread_mutex_lock(&mutex);
    sharedCounter++;  // critical section
    pthread_mutex_unlock(&mutex);
    return NULL;
}
// Compile: gcc -pthread program.c
\`\`\`

### Sockets — Network Programming

\`\`\`c
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>

// Simple TCP server
int server_fd = socket(AF_INET, SOCK_STREAM, 0);

struct sockaddr_in addr = {
    .sin_family      = AF_INET,
    .sin_port        = htons(8080),
    .sin_addr.s_addr = INADDR_ANY,
};

bind(server_fd, (struct sockaddr*)&addr, sizeof(addr));
listen(server_fd, 5);

int client_fd = accept(server_fd, NULL, NULL);
char buf[1024];
recv(client_fd, buf, sizeof(buf), 0);
send(client_fd, "HTTP/1.1 200 OK\\r\\n\\r\\nHello!", 26, 0);
close(client_fd);
close(server_fd);
\`\`\`

### setjmp/longjmp — Exception-like Error Handling

\`\`\`c
#include <setjmp.h>
jmp_buf jumpBuffer;

void riskyFunc() {
    printf("About to fail...\\n");
    longjmp(jumpBuffer, 1);  // jump back!
}

int main() {
    if (setjmp(jumpBuffer) == 0) {
        printf("Trying risky function...\\n");
        riskyFunc();
        printf("This won't print\\n");
    } else {
        printf("Recovered from error!\\n");
    }
    return 0;
}
\`\`\`

### volatile aur restrict

\`\`\`c
// volatile: tell compiler not to optimize this variable
// (value can change outside of program control)
volatile int hardwareRegister = 0;
volatile int* const PORT = (volatile int*)0xFFFF0000;
// Used in: embedded systems, signal handlers, hardware interfacing

// restrict: pointer is the ONLY way to access this data
// Allows compiler to make aggressive optimizations
void add(float *restrict a, float *restrict b, float *restrict c, int n) {
    for (int i = 0; i < n; i++) c[i] = a[i] + b[i];
    // Compiler knows a, b, c don't overlap → SIMD possible!
}
\`\`\`

### Generics with _Generic (C11)

\`\`\`c
// Type-safe "generic" functions
#define ABS(x) _Generic((x),    \\
    int:    abs,                 \\
    long:   labs,                \\
    float:  fabsf,               \\
    double: fabs                 \\
)(x)

#define MAX(a,b) _Generic((a),  \\
    int:    imax,                \\
    float:  fmaxf,               \\
    double: fmax                 \\
)(a,b)

// Usage — type-safe!
ABS(-5);       // calls abs()
ABS(-3.14f);   // calls fabsf()
ABS(-2.718);   // calls fabs()
\`\`\`

### Useful C99/C11/C17 Features

\`\`\`c
// C99: Variable Length Arrays
void func(int n) {
    int arr[n];  // size at runtime! (be careful — stack)
}

// C99: Compound literals
void draw(struct Point p) { }
draw((struct Point){.x=10, .y=20});  // temporary struct

// C11: Anonymous structs/unions
typedef struct {
    union {
        struct { int x, y; };    // anonymous!
        int coords[2];
    };
} Point;
Point p; p.x = 1; p.coords[1] = 2;  // both work!

// C11: _Alignas, _Alignof
_Alignas(16) float simdData[4];  // 16-byte aligned for SSE

// Designated initializers (C99)
int arr[10] = {[3]=30, [7]=70};  // only indices 3 and 7 set

typedef struct { int a, b, c; } S;
S s = {.b=20, .c=30};  // a defaults to 0
\`\`\``,

      content_en: `## Advanced C — Industry-Level Concepts!

### POSIX Threads — Multithreading in C

\`\`\`c
#include <pthread.h>

// Thread function
void* threadFunc(void *arg) {
    int id = *(int*)arg;
    printf("Thread %d running\\n", id);
    // do work...
    return NULL;
}

// Create and join threads
pthread_t threads[4];
int ids[4] = {1, 2, 3, 4};

for (int i = 0; i < 4; i++)
    pthread_create(&threads[i], NULL, threadFunc, &ids[i]);

for (int i = 0; i < 4; i++)
    pthread_join(threads[i], NULL);  // wait for all

// Mutex — protect shared data
pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;
int sharedCounter = 0;

void* safeIncrement(void *arg) {
    pthread_mutex_lock(&mutex);
    sharedCounter++;  // critical section
    pthread_mutex_unlock(&mutex);
    return NULL;
}
// Compile: gcc -pthread program.c
\`\`\`

### Sockets — Network Programming

\`\`\`c
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>

// Simple TCP server
int server_fd = socket(AF_INET, SOCK_STREAM, 0);

struct sockaddr_in addr = {
    .sin_family      = AF_INET,
    .sin_port        = htons(8080),
    .sin_addr.s_addr = INADDR_ANY,
};

bind(server_fd, (struct sockaddr*)&addr, sizeof(addr));
listen(server_fd, 5);

int client_fd = accept(server_fd, NULL, NULL);
char buf[1024];
recv(client_fd, buf, sizeof(buf), 0);
send(client_fd, "HTTP/1.1 200 OK\\r\\n\\r\\nHello!", 26, 0);
close(client_fd);
close(server_fd);
\`\`\`

### setjmp/longjmp — Exception-like Error Handling

\`\`\`c
#include <setjmp.h>
jmp_buf jumpBuffer;

void riskyFunc() {
    printf("About to fail...\\n");
    longjmp(jumpBuffer, 1);  // jump back!
}

int main() {
    if (setjmp(jumpBuffer) == 0) {
        printf("Trying risky function...\\n");
        riskyFunc();
        printf("This won't print\\n");
    } else {
        printf("Recovered from error!\\n");
    }
    return 0;
}
\`\`\`

### volatile and restrict

\`\`\`c
// volatile: tell compiler not to optimize this variable
// (value can change outside of program control)
volatile int hardwareRegister = 0;
volatile int* const PORT = (volatile int*)0xFFFF0000;
// Used in: embedded systems, signal handlers, hardware interfacing

// restrict: pointer is the ONLY way to access this data
// Allows compiler to make aggressive optimizations
void add(float *restrict a, float *restrict b, float *restrict c, int n) {
    for (int i = 0; i < n; i++) c[i] = a[i] + b[i];
    // Compiler knows a, b, c don't overlap → SIMD possible!
}
\`\`\`

### Generics with _Generic (C11)

\`\`\`c
// Type-safe "generic" functions
#define ABS(x) _Generic((x),    \\
    int:    abs,                 \\
    long:   labs,                \\
    float:  fabsf,               \\
    double: fabs                 \\
)(x)

#define MAX(a,b) _Generic((a),  \\
    int:    imax,                \\
    float:  fmaxf,               \\
    double: fmax                 \\
)(a,b)

// Usage — type-safe!
ABS(-5);       // calls abs()
ABS(-3.14f);   // calls fabsf()
ABS(-2.718);   // calls fabs()
\`\`\`

### Useful C99/C11/C17 Features

\`\`\`c
// C99: Variable Length Arrays
void func(int n) {
    int arr[n];  // size at runtime! (be careful — stack)
}

// C99: Compound literals
void draw(struct Point p) { }
draw((struct Point){.x=10, .y=20});  // temporary struct

// C11: Anonymous structs/unions
typedef struct {
    union {
        struct { int x, y; };    // anonymous!
        int coords[2];
    };
} Point;
Point p; p.x = 1; p.coords[1] = 2;  // both work!

// C11: _Alignas, _Alignof
_Alignas(16) float simdData[4];  // 16-byte aligned for SSE

// Designated initializers (C99)
int arr[10] = {[3]=30, [7]=70};  // only indices 3 and 7 set

typedef struct { int a, b, c; } S;
S s = {.b=20, .c=30};  // a defaults to 0
\`\`\``,

      codeExample: `#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <string.h>
#include <time.h>

// ── Advanced C feature demos ──

// 1. Designated initializers (C99)
typedef struct {
    char  name[30];
    int   age;
    float gpa;
    int   enrolled;
} Student;

// 2. Flexible array member (C99)
typedef struct {
    int  count;
    int  capacity;
    int  data[];  // flexible array — must be last!
} DynArray;

DynArray* dynCreate(int cap) {
    DynArray *a = malloc(sizeof(DynArray) + cap * sizeof(int));
    a->count = 0; a->capacity = cap;
    return a;
}
void dynPush(DynArray **ap, int v) {
    DynArray *a = *ap;
    if (a->count == a->capacity) {
        a->capacity *= 2;
        *ap = realloc(a, sizeof(DynArray) + a->capacity * sizeof(int));
        a = *ap;
    }
    a->data[a->count++] = v;
}

// 3. Compound literals + designated init
void printStudentInfo(Student s) {
    printf("Name: %s, Age: %d, GPA: %.2f\\n", s.name, s.age, s.gpa);
}

// 4. Function pointer table pattern
typedef int (*MathFn)(int,int);
int add(int a,int b){return a+b;}
int sub(int a,int b){return a-b;}
int mul(int a,int b){return a*b;}
int mod(int a,int b){return b?a%b:-1;}

const char *fnNames[] = {"add","sub","mul","mod"};
MathFn fnTable[] = {add,sub,mul,mod};
int fnCount = 4;

// 5. Bit manipulation utility
uint32_t reverseBits(uint32_t n) {
    uint32_t r=0;
    for(int i=0;i<32;i++){r=(r<<1)|(n&1);n>>=1;}
    return r;
}
int parityBit(uint32_t n) {
    n ^= n>>16; n ^= n>>8; n ^= n>>4;
    return (0x6996>>n&0xF)&1;  // lookup nibble parity
}

// 6. Memory alignment check
int isAligned(void *ptr, size_t align) {
    return ((uintptr_t)ptr & (align-1)) == 0;
}

int main(){
    printf("=== Advanced C Features ===\\n\\n");

    // Designated initializers
    Student students[] = {
        {.name="Rahul", .age=20, .gpa=8.5f, .enrolled=1},
        {.name="Priya", .age=19, .gpa=9.2f, .enrolled=1},
        {.name="Aryan", .age=21, .gpa=7.8f, .enrolled=0},
    };
    printf("1. Designated Initializers:\\n");
    for(int i=0;i<3;i++) printStudentInfo(students[i]);

    // Compound literal
    printf("\\n2. Compound Literal:\\n");
    printStudentInfo((Student){.name="Temporary", .age=18, .gpa=7.5f});

    // Flexible array member
    printf("\\n3. Flexible Array Member:\\n");
    DynArray *arr = dynCreate(2);
    for(int i=1;i<=8;i++) dynPush(&arr,i*10);
    printf("Array: ");
    for(int i=0;i<arr->count;i++) printf("%d ",arr->data[i]);
    printf("(cap=%d)\\n",arr->capacity);
    free(arr);

    // Function pointer table
    printf("\\n4. Function Pointer Table:\\n");
    int a=15, b=4;
    for(int i=0;i<fnCount;i++)
        printf("  %s(%d,%d) = %d\\n",fnNames[i],a,b,fnTable[i](a,b));

    // Bit operations
    printf("\\n5. Bit Manipulation:\\n");
    uint32_t n=0xABCD1234;
    printf("  Original : 0x%08X\\n",n);
    printf("  Reversed : 0x%08X\\n",reverseBits(n));
    printf("  Parity   : %d\\n",parityBit(n));
    printf("  Pop count: %d\\n",__builtin_popcount(n));
    printf("  CLZ      : %d\\n",__builtin_clz(n));

    // Alignment
    printf("\\n6. Memory Alignment:\\n");
    int   x=42;
    float f=3.14f;
    double d=2.718;
    printf("  int    %p aligned to 4: %s\\n",(void*)&x, isAligned(&x,4)?"Yes":"No");
    printf("  float  %p aligned to 4: %s\\n",(void*)&f, isAligned(&f,4)?"Yes":"No");
    printf("  double %p aligned to 8: %s\\n",(void*)&d, isAligned(&d,8)?"Yes":"No");

    printf("\\n✅ Advanced features demonstrated!\\n");
    return 0;
}`,

      codeExample_en: `#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

// Flexible array member
typedef struct { int n,cap; int data[]; } DA;
DA* daNew(int c){ DA*a=malloc(sizeof(DA)+c*4); a->n=0;a->cap=c;return a; }
void daPush(DA**p,int v){
    DA*a=*p;
    if(a->n==a->cap){a->cap*=2;*p=realloc(a,sizeof(DA)+a->cap*4);a=*p;}
    a->data[a->n++]=v;
}

// Designated initializers
typedef struct{char name[20];int age;float gpa;}S;

int main(){
    S arr[]={{.name="Alice",.age=20,.gpa=9.0f},{.name="Bob",.age=21,.gpa=8.0f}};
    for(int i=0;i<2;i++) printf("%s %.1f\\n",arr[i].name,arr[i].gpa);
    
    DA*da=daNew(2); for(int i=0;i<6;i++) daPush(&da,i*10);
    for(int i=0;i<da->n;i++) printf("%d ",da->data[i]); printf("\\n");
    free(da);
    
    printf("popcount(255) = %d\\n",__builtin_popcount(255));
    return 0;
}`,

      task: {
        description: 'Advanced C practice: (1) "Thread-safe Counter" — pthreads se 4 threads banao jo ek shared counter increment karein, mutex se race condition prevent karo, result verify karo. (2) "Simple HTTP Server" — socket programming se basic HTTP server banao jo "Hello, World!" return kare. (3) "Generic Stack" — _Generic ya void* se type-agnostic stack banao int, float, char* sab ke liye. (4) "Endian Converter" — big-endian aur little-endian ke beech integers convert karo, system endianness detect karo.',
        description_en: 'Advanced C practice: (1) "Thread-safe Counter" — create 4 pthreads that all increment a shared counter, prevent race conditions with a mutex, verify the result. (2) "Simple HTTP Server" — use socket programming to build a basic HTTP server that returns "Hello, World!". (3) "Generic Stack" — use _Generic or void* to build a type-agnostic stack for int, float, and char*. (4) "Endian Converter" — convert integers between big-endian and little-endian, detect system endianness.',
        hint: 'Thread-safe: each thread runs 100000 increments, final should be 400000. Without mutex it will be less! HTTP server: send "HTTP/1.1 200 OK\\r\\nContent-Length: 13\\r\\n\\r\\nHello, World!". Endian: union{int i;char b[4];}; b[0]==1 means little-endian.',
        hint_en: 'Thread-safe: each thread runs 100,000 increments, the final count should be 400,000. Without a mutex it will be less! HTTP server: send "HTTP/1.1 200 OK\\r\\nContent-Length: 13\\r\\n\\r\\nHello, World!". Endian: union{int i;char b[4];}; b[0]==1 means little-endian.',
      },
      quiz: [
        {
          q: 'volatile keyword C mein kyun important hai embedded programming mein?',
          options: [
            'Speed ke liye',
            'Compiler ko batata hai: yeh variable bahar se change ho sakta hai — optimize mat karo. Hardware registers, signal handlers, interrupt-modified variables ke liye essential. Bina volatile: compiler dead-store elimination karta hai.',
            'Memory save karne ke liye',
            'Debugging ke liye',
          ],
          correct: 1,
          explanation: 'Compiler optimization: agar code same variable repeatedly read karta hai bina changing, compiler register mein cache kar sakta hai (no memory reads). Hardware registers: value CPU se independent change hoti hai! volatile = "always re-read from memory". Without volatile: while(reg != 0) {} could be while(true) {} (optimized!). Interrupt handlers: volatile sig_atomic_t flag = 0; in signal handler.',
          q_en: 'Why is the volatile keyword important in embedded programming in C?',
          options_en: [
            'For speed',
            'It tells the compiler: this variable can change from outside — do not optimise it. Essential for hardware registers, signal handlers, and interrupt-modified variables. Without volatile: the compiler can eliminate "dead stores".',
            'To save memory',
            'For debugging',
          ],
          explanation_en: "Compiler optimisation: if code reads the same variable repeatedly without changing it, the compiler may cache it in a register (no memory reads). Hardware registers: the value changes independently of the CPU! volatile = 'always re-read from memory'. Without volatile: while(reg != 0) {} could be compiled as while(true) {} (optimised away!). Interrupt handlers: volatile sig_atomic_t flag = 0; in a signal handler.",
        },
        {
          q: 'Flexible Array Member C99 mein kya advantage deta hai?',
          options: [
            'Koi advantage nahi',
            'Struct aur uska variable-size data ek single malloc mein — better cache locality, fewer allocations. sizeof(struct) = just header size. malloc(sizeof + n*elem_size) — header + data contiguous!',
            'Static size ke liye',
            'Type safety ke liye',
          ],
          correct: 1,
          explanation: 'Without FAM: struct + separate malloc for data = 2 allocations + pointer = cache miss when accessing data. With FAM: struct { int n; int data[]; }. malloc(sizeof(S) + n*4) = ONE allocation, data immediately after struct in memory. Cache-friendly! Use: network packets, variable-length messages, string buffers. Always last member, cannot use in arrays of that struct.',
          q_en: 'What advantage does a Flexible Array Member give in C99?',
          options_en: [
            'No advantage',
            'The struct and its variable-size data in a single malloc — better cache locality, fewer allocations. sizeof(struct) = just the header. malloc(sizeof + n*elem_size) — header + data are contiguous!',
            'For static sizing',
            'For type safety',
          ],
          explanation_en: 'Without FAM: struct + separate malloc for data = 2 allocations + a pointer = cache miss when accessing data. With FAM: struct { int n; int data[]; }. malloc(sizeof(S) + n*4) = ONE allocation, data immediately after the struct in memory. Cache-friendly! Uses: network packets, variable-length messages, string buffers. Must always be the last member; cannot use in arrays of that struct.',
        },
        {
          q: 'Race condition kya hai aur C mein kaise prevent karte hain?',
          options: [
            'CPU speed issue hai',
            'Do threads ek shared resource simultaneously access karte hain without synchronization — result unpredictable. Fix: mutex (mutual exclusion) — sirf ek thread critical section mein ek time pe.',
            'Compile error hai',
            'Memory leak hai',
          ],
          correct: 1,
          explanation: 'Race condition: Thread A reads counter=5, Thread B reads counter=5, A increments to 6, B increments to 6. Expected: 7, Actual: 6! Mutex: lock → critical section → unlock. Only one thread executes critical section at a time. Other patterns: semaphores (counting), read-write locks (multiple readers, one writer), atomic operations (__atomic_add_fetch — no mutex needed for simple ops).',
          q_en: 'What is a race condition and how do you prevent it in C?',
          options_en: [
            'A CPU speed issue',
            'Two threads access a shared resource simultaneously without synchronisation — the result is unpredictable. Fix: mutex (mutual exclusion) — only one thread is in the critical section at a time.',
            'A compile error',
            'A memory leak',
          ],
          explanation_en: 'Race condition: Thread A reads counter=5, Thread B reads counter=5, A increments to 6, B increments to 6. Expected: 7, Actual: 6! Mutex: lock → critical section → unlock. Only one thread executes the critical section at a time. Other patterns: semaphores (counting), read-write locks (multiple readers, one writer), atomic operations (__atomic_add_fetch — no mutex needed for simple ops).',
        },
      ],
    },

    // ── SECTION 4 ──────────────────────────────────────────────────────
    {
      id: 'c-w12-s4',
      title: '🎓 Certificate — C Programming Master!',
      title_en: '🎓 Certificate — C Programming Master!',
      emoji: '🏆',
      content: `## 🎓 Congratulations — C Programming Master!

### 12 Hafte mein Kya Achieve Kiya?

\`\`\`
✅ Month 1 — C Foundation
  • Variables, operators, control flow, functions
  • Arrays, strings, pointers, dynamic memory
  • Recursion, file I/O basics

✅ Month 2 — Data Structures
  • struct, union, enum, linked lists
  • Stack, Queue, Hash Table
  • Binary files, error handling
  • Preprocessor, macros, bitwise ops

✅ Month 3 — Advanced Algorithms
  • Binary Trees, BST, AVL Tree
  • 8 Sorting Algorithms (O(n²) to O(n))
  • Graphs — DFS/BFS/Dijkstra/MST/TopSort
  • Production-ready system design

Total: 12 weeks × 4 sections = 48 sections
       48 × 3 quiz questions = 144 quiz questions
       12 hands-on projects
\`\`\`

### Skills Unlocked

\`\`\`
🔓 Systems Programming
    C ka foundation → Linux, drivers, embedded

🔓 Competitive Programming
    O(n²) se O(n) sorts, O(logn) trees, O(V+E) graphs

🔓 Software Engineering
    Clean code, error handling, modular design

🔓 Computer Science Theory
    Big-O, space-time trade-offs, algorithm design

🔓 Problem Solving
    Any DS/Algo problem break karo aur solve karo
\`\`\`

### Next Steps — C ke Baad

\`\`\`
🚀 Immediate Next:
  • C++ (OOP + STL + modern C++)
  • Competitive Programming (Codeforces/LeetCode)
  • System Design (OS, networks, databases)

🚀 Career Paths:
  • Systems Programmer (Linux, embedded, RTOS)
  • Backend Engineer (performance-critical systems)
  • Game Developer (game engines, graphics)
  • Compiler Engineer (GCC, LLVM)
  • Security Researcher (vulnerability research)
  • Embedded Engineer (IoT, automotive, aerospace)

🚀 Resources:
  • "The C Programming Language" by K&R (Bible!)
  • "Programming Pearls" by Jon Bentley
  • Linux kernel source code (github.com/torvalds/linux)
  • Codeforces / LeetCode for practice
\`\`\`

### Certificate Achievement!

\`\`\`
╔══════════════════════════════════════════════════════════╗
║          🎓 STUDYEARN AI CERTIFICATE                     ║
║                                                          ║
║   This certifies that the student has successfully       ║
║   completed the                                          ║
║                                                          ║
║        C PROGRAMMING — NOOB TO PRO                       ║
║                                                          ║
║   12-Week Comprehensive Course covering:                 ║
║   ✓ C Foundation (Months 1)                              ║
║   ✓ Data Structures (Month 2)                            ║
║   ✓ Advanced Algorithms (Month 3)                        ║
║                                                          ║
║   Skills: Pointers, Memory Management, DS & Algorithms   ║
║   Level: Intermediate to Advanced                        ║
║                                                          ║
║              StudyEarn AI Platform                       ║
╚══════════════════════════════════════════════════════════╝
\`\`\``,

      content_en: `## 🎓 Congratulations — C Programming Master!

### 12 Hafte in Kya Achieve Kiya?

\`\`\`
✅ Month 1 — C Foundation
  • Variables, operators, control flow, functions
  • Arrays, strings, pointers, dynamic memory
  • Recursion, file I/O basics

✅ Month 2 — Data Structures
  • struct, union, enum, linked lists
  • Stack, Queue, Hash Table
  • Binary files, error handling
  • Preprocessor, macros, bitwise ops

✅ Month 3 — Advanced Algorithms
  • Binary Trees, BST, AVL Tree
  • 8 Sorting Algorithms (O(n²) to O(n))
  • Graphs — DFS/BFS/Dijkstra/MST/TopSort
  • Production-ready system design

Total: 12 weeks × 4 sections = 48 sections
       48 × 3 quiz questions = 144 quiz questions
       12 hands-on projects
\`\`\`

### Skills Unlocked

\`\`\`
🔓 Systems Programming
    C ka foundation → Linux, drivers, embedded

🔓 Competitive Programming
    O(n²) se O(n) sorts, O(logn) trees, O(V+E) graphs

🔓 Software Engineering
    Clean code, error handling, modular design

🔓 Computer Science Theory
    Big-O, space-time trade-offs, algorithm design

🔓 Problem Solving
    Any DS/Algo problem break do and solve do
\`\`\`

### Next Steps — C After

\`\`\`
🚀 Immediate Next:
  • C++ (OOP + STL + modern C++)
  • Competitive Programming (Codeforces/LeetCode)
  • System Design (OS, networks, databases)

🚀 Career Paths:
  • Systems Programmer (Linux, embedded, RTOS)
  • Backend Engineer (performance-critical systems)
  • Game Developer (game engines, graphics)
  • Compiler Engineer (GCC, LLVM)
  • Security Researcher (vulnerability research)
  • Embedded Engineer (IoT, automotive, aerospace)

🚀 Resources:
  • "The C Programming Language" by K&R (Bible!)
  • "Programming Pearls" by Jon Bentley
  • Linux kernel source code (github.com/torvalds/linux)
  • Codeforces / LeetCode for practice
\`\`\`

### Certificate Achievement!

\`\`\`
╔══════════════════════════════════════════════════════════╗
║          🎓 STUDYEARN AI CERTIFICATE                     ║
║                                                          ║
║   This certifies that the student has successfully       ║
║   completed the                                          ║
║                                                          ║
║        C PROGRAMMING — NOOB TO PRO                       ║
║                                                          ║
║   12-Week Comprehensive Course covering:                 ║
║   ✓ C Foundation (Months 1)                              ║
║   ✓ Data Structures (Month 2)                            ║
║   ✓ Advanced Algorithms (Month 3)                        ║
║                                                          ║
║   Skills: Pointers, Memory Management, DS & Algorithms   ║
║   Level: Intermediate to Advanced                        ║
║                                                          ║
║              StudyEarn AI Platform                       ║
╚══════════════════════════════════════════════════════════╝
\`\`\``,

      codeExample: `#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

// ════════════════════════════════════════════════════════════════
//  🎓 STUDYEARN C PROGRAMMING — FINAL SHOWCASE
//  This program demonstrates ALL major concepts from 12 weeks
// ════════════════════════════════════════════════════════════════

// ── Week 4: Pointers + Dynamic Memory ──
typedef struct Node { int d; struct Node *n; } Node;
Node* ll_push(Node *h, int d){ Node*n=malloc(sizeof*n); n->d=d; n->n=h; return n; }
void ll_free(Node *h){ while(h){Node*t=h->n;free(h);h=t;} }

// ── Week 8: Hash Table ──
#define HS 32
typedef struct E{char k[20];int v;struct E*n;}E;
typedef struct{E*b[HS];int cnt;}HM;
HM* hm_new(){return calloc(1,sizeof(HM));}
static unsigned hsh(const char*s){unsigned h=5381;while(*s)h=h*33+(unsigned char)*s++;return h%HS;}
void hm_set(HM*m,const char*k,int v){unsigned i=hsh(k);for(E*e=m->b[i];e;e=e->n)if(!strcmp(e->k,k)){e->v=v;return;}E*e=malloc(sizeof*e);strncpy(e->k,k,19);e->v=v;e->n=m->b[i];m->b[i]=e;m->cnt++;}
int hm_get(HM*m,const char*k,int d){for(E*e=m->b[hsh(k)];e;e=e->n)if(!strcmp(e->k,k))return e->v;return d;}
void hm_free(HM*m){for(int i=0;i<HS;i++){E*e=m->b[i],*t;while(e){t=e->n;free(e);e=t;}}free(m);}

// ── Week 9: BST ──
typedef struct BST{int d;struct BST*l,*r;}BST;
BST* bst_ins(BST*r,int v){if(!r){BST*n=malloc(sizeof*n);n->d=v;n->l=n->r=NULL;return n;}if(v<r->d)r->l=bst_ins(r->l,v);else if(v>r->d)r->r=bst_ins(r->r,v);return r;}
void bst_io(BST*r){if(!r)return;bst_io(r->l);printf("%d ",r->d);bst_io(r->r);}
void bst_free(BST*r){if(!r)return;bst_free(r->l);bst_free(r->r);free(r);}

// ── Week 10: Merge Sort ──
void merge(int a[],int l,int m,int r){int n1=m-l+1,n2=r-m;int*L=malloc(n1*4),*R=malloc(n2*4);for(int i=0;i<n1;i++)L[i]=a[l+i];for(int i=0;i<n2;i++)R[i]=a[m+1+i];int i=0,j=0,k=l;while(i<n1&&j<n2)a[k++]=L[i]<=R[j]?L[i++]:R[j++];while(i<n1)a[k++]=L[i++];while(j<n2)a[k++]=R[j++];free(L);free(R);}
void msort(int a[],int l,int r){if(l>=r)return;int m=l+(r-l)/2;msort(a,l,m);msort(a,m+1,r);merge(a,l,m,r);}

// ── Week 11: BFS on graph ──
#define MV 6
typedef struct GN{int v;struct GN*n;}GN;
typedef struct{GN*h[MV];int V;}G;
G* g_new(int V){G*g=calloc(1,sizeof(G));g->V=V;return g;}
void g_ae(G*g,int u,int v){GN*n=malloc(sizeof*n);n->v=v;n->n=g->h[u];g->h[u]=n;GN*m=malloc(sizeof*m);m->v=u;m->n=g->h[v];g->h[v]=m;}
void bfs(G*g,int s){int q[MV],vis[MV]={0},f=0,r=0;vis[s]=1;q[r++]=s;printf("BFS: ");while(f<r){int v=q[f++];printf("%d ",v);for(GN*n=g->h[v];n;n=n->n)if(!vis[n->v]){vis[n->v]=1;q[r++]=n->v;}}}

void printBanner() {
    printf("\\n");
    printf("  ╔══════════════════════════════════════════════════╗\\n");
    printf("  ║  🎓 C PROGRAMMING MASTER — FINAL SHOWCASE       ║\\n");
    printf("  ║     StudyEarn AI — 12 Weeks Complete!           ║\\n");
    printf("  ╚══════════════════════════════════════════════════╝\\n\\n");
}

int main(){
    printBanner();

    // Week 4: Linked List (pointers + dynamic memory)
    printf("📌 Linked List (Week 4 — Pointers):\\n  ");
    Node *list=NULL;
    for(int i=5;i>=1;i--) list=ll_push(list,i*10);
    for(Node*n=list;n;n=n->n) printf("%d%s",n->d,n->n?"→":"\\n");
    ll_free(list);

    // Week 8: Hash Table
    printf("\\n📌 Hash Table (Week 8 — O(1) lookup):\\n");
    HM *hm=hm_new();
    hm_set(hm,"C",95); hm_set(hm,"Python",88); hm_set(hm,"Java",82);
    hm_set(hm,"C++",90); hm_set(hm,"Go",85);
    printf("  C:%d Python:%d Java:%d C++:%d Go:%d\\n",
           hm_get(hm,"C",0),hm_get(hm,"Python",0),
           hm_get(hm,"Java",0),hm_get(hm,"C++",0),hm_get(hm,"Go",0));
    hm_free(hm);

    // Week 9: BST
    printf("\\n📌 BST Inorder (Week 9 — Trees):\\n  ");
    BST *tree=NULL;
    int vals[]={50,30,70,20,40,60,80,10,25};
    for(int i=0;i<9;i++) tree=bst_ins(tree,vals[i]);
    bst_io(tree); printf("\\n");
    bst_free(tree);

    // Week 10: Merge Sort
    printf("\\n📌 Merge Sort (Week 10 — Sorting):\\n");
    int arr[]={64,34,25,12,22,11,90,45,78,35};
    int n=sizeof(arr)/sizeof(arr[0]);
    printf("  Before: "); for(int i=0;i<n;i++) printf("%d ",arr[i]);
    msort(arr,0,n-1);
    printf("\\n  After:  "); for(int i=0;i<n;i++) printf("%d ",arr[i]);
    printf("\\n");

    // Week 11: Graph BFS
    printf("\\n📌 Graph BFS (Week 11 — Graphs):\\n  ");
    G *g=g_new(6);
    g_ae(g,0,1); g_ae(g,0,2); g_ae(g,1,3);
    g_ae(g,2,4); g_ae(g,3,5); g_ae(g,4,5);
    bfs(g,0); printf("\\n");

    // Certificate
    printf("\\n");
    printf("  ╔══════════════════════════════════════════════════╗\\n");
    printf("  ║   🏆 CERTIFICATE OF COMPLETION                  ║\\n");
    printf("  ║                                                  ║\\n");
    printf("  ║   C Programming — Noob to Pro                   ║\\n");
    printf("  ║   12 Weeks | 48 Sections | 12 Projects          ║\\n");
    printf("  ║                                                  ║\\n");
    printf("  ║   Skills Mastered:                               ║\\n");
    printf("  ║   ✓ Memory Management & Pointers                 ║\\n");
    printf("  ║   ✓ Data Structures (DS & Algorithms)            ║\\n");
    printf("  ║   ✓ Trees, Sorting, Graphs                       ║\\n");
    printf("  ║   ✓ File I/O & Systems Programming               ║\\n");
    printf("  ║                                                  ║\\n");
    printf("  ║         🎓 StudyEarn AI Platform                 ║\\n");
    printf("  ╚══════════════════════════════════════════════════╝\\n");
    printf("\\n  Congratulations! Keep coding! 🚀\\n\\n");

    return 0;
}`,

      codeExample_en: `/* See above — complete 12-week showcase + certificate */`,

      task: {
        description: 'FINAL CHALLENGE — Prove your C mastery: (1) "Complete Mini OS Simulator" — processes ko queue mein rakho, CPU scheduling implement karo (Round Robin + Priority), memory allocation simulate karo (buddy system basic). (2) "Compress a File" — Huffman coding implement karo — character frequency count karo, min-heap se Huffman tree banao, encoding table banao, file compress karo. (3) "Write your own malloc" — sbrk() system call se memory pool banao, first-fit allocation implement karo. Agar yeh sab karo — true C master ho!',
        description_en: 'FINAL CHALLENGE — Prove your C mastery: (1) "Complete Mini OS Simulator" — queue processes, implement CPU scheduling (Round Robin + Priority), simulate memory allocation (basic buddy system). (2) "Compress a File" — implement Huffman coding — count character frequencies, build a Huffman tree with a min-heap, build the encoding table, compress the file. (3) "Write your own malloc" — build a memory pool using the sbrk() system call, implement first-fit allocation. If you do all of this — you are a true C master!',
        hint: 'Huffman: min-heap of (freq,char), combine two minimums into internal node, repeat. DFS to build encoding table. Write compressed bits to file with bit manipulation. Mini malloc: large block from OS, split for allocations, free list for returned blocks.',
        hint_en: 'Huffman: min-heap of (freq,char), combine the two minimums into an internal node, repeat until one node remains. DFS to build the encoding table. Write compressed bits to file using bit manipulation. Mini malloc: get a large block from the OS, split for allocations, maintain a free list for returned blocks.',
      },
      quiz: [
        {
          q: '12 weeks mein C seekhne ke baad, kaunsi language sabse easily samajh aayegi?',
          options: [
            'JavaScript',
            'C++ — C ka superset hai. Classes, templates, STL (vector, map, sort) = C ke concepts pe build. C++ mein C code directly compile hota hai. RAII, smart pointers = C ke manual memory ka automated version.',
            'Python',
            'Java',
          ],
          correct: 1,
          explanation: 'C++ is built on top of C. Everything you learned: (1) Pointers → C++ references + smart pointers. (2) malloc/free → new/delete → unique_ptr/shared_ptr. (3) struct → class (add methods + access control). (4) Function pointers → std::function + lambdas. (5) Arrays → std::vector. (6) Our hash table → std::unordered_map. C++ = C + OOP + templates + standard library. Transition: easy!',
          q_en: 'After learning C for 12 weeks, which language will be easiest to understand?',
          options_en: [
            'JavaScript',
            'C++ — it is a superset of C. Classes, templates, STL (vector, map, sort) = built on C concepts. C++ compiles C code directly. RAII, smart pointers = automated versions of C\'s manual memory.',
            'Python',
            'Java',
          ],
          explanation_en: "C++ is built on top of C. Everything you learned: (1) Pointers → C++ references + smart pointers. (2) malloc/free → new/delete → unique_ptr/shared_ptr. (3) struct → class (add methods + access control). (4) Function pointers → std::function + lambdas. (5) Arrays → std::vector. (6) Our hash table → std::unordered_map. C++ = C + OOP + templates + standard library. The transition: easy!",
        },
        {
          q: 'Huffman coding mein min-heap kyun use karte hain?',
          options: [
            'Sirf tradition',
            'Sabse kam frequency wale do nodes ko combine karo → new internal node. Min-heap O(log n) minimum extract karta hai. Na karo toh: O(n) scan × n iterations = O(n²). Heap se: O(n log n). Greedy optimal.',
            'Max-heap bhi kaam karta hai',
            'Array se kaam nahi hota',
          ],
          correct: 1,
          explanation: 'Huffman algorithm: Greedy — always combine 2 lowest frequency nodes. Min-heap: extract 2 minimums O(log n) each, insert combined node O(log n). Total: O(n log n). Without heap: find minimum = O(n) scan, n iterations = O(n²). Greedy correctness: optimal prefix code. Used in: zlib, PNG, JPEG, HTTP/2 HPACK header compression.',
          q_en: 'Why do we use a min-heap in Huffman coding?',
          options_en: [
            'Just tradition',
            'Combine the two lowest-frequency nodes → new internal node. Min-heap extracts the minimum in O(log n). Without it: O(n) scan × n iterations = O(n²). With heap: O(n log n). Greedy optimal.',
            'A max-heap also works',
            'Cannot be done with an array',
          ],
          explanation_en: "Huffman algorithm: Greedy — always combine the 2 lowest-frequency nodes. Min-heap: extract 2 minimums O(log n) each, insert combined node O(log n). Total: O(n log n). Without heap: find minimum = O(n) scan × n iterations = O(n²). Greedy correctness: produces an optimal prefix code. Used in: zlib, PNG, JPEG, HTTP/2 HPACK header compression.",
        },
        {
          q: 'C programming course complete karne ka sabse important takeaway kya hai?',
          options: [
            'C fastest language hai',
            'Computer ka mental model clear hua — memory, CPU, OS sab samajh aaya. Yeh foundation kisi bhi language, system ya algorithm samajhne ke liye kaam aayegi. Programming = problem solving, language = tool.',
            'Pointers yaad ho gaye',
            'Sorting algorithms yaad ho gaye',
          ],
          correct: 1,
          explanation: 'C programming ki deepest lesson: computers ek abstraction nahi hain — actual bytes, addresses, instructions hain. Jab Python mein list.append() slow lagta hai, ab samajhte ho kyun (dynamic array realloc). Jab Java throws OutOfMemoryError, samajhte ho heap kya hai. Jab network timeout hota hai, buffer aur sockets samajh aate hain. C = X-ray vision for computers!',
          q_en: 'What is the most important takeaway from completing the C programming course?',
          options_en: [
            'C is the fastest language',
            'Your mental model of computers is now clear — memory, CPU, OS all understood. This foundation will help you understand any language, system, or algorithm. Programming = problem solving; language = tool.',
            'Pointers are memorised',
            'Sorting algorithms are memorised',
          ],
          explanation_en: "C programming's deepest lesson: computers are not an abstraction — they are actual bytes, addresses, and instructions. When Python's list.append() feels slow, now you know why (dynamic array realloc). When Java throws OutOfMemoryError, you understand what the heap is. When a network times out, you understand buffers and sockets. C = X-ray vision for computers!",
        },
      ],
    },
  ],
};